"""
论文分析 Prompt 模板库
覆盖: 快速扫描 / 标准分析 / 深度分析 / 对比分析
"""

SYSTEM_PROMPT = """你是一位顶尖的科研论文分析专家，拥有计算机科学、人工智能、数学、物理等多学科背景。
你的分析精准、深刻、客观，能从工程实现与学术贡献两个维度解读论文。
请始终以中文回复，保持专业而不失可读性。"""


def build_quick_prompt(text: str) -> str:
    excerpt = text[:6000]
    return f"""请对以下论文进行**快速扫描分析**，以 JSON 格式输出，包含如下字段：

```json
{{
  "title": "论文标题",
  "authors": "作者信息",
  "venue": "发表期刊/会议",
  "year": "年份",
  "problem": "解决的核心问题（1-2句）",
  "key_contributions": ["贡献1", "贡献2", "贡献3"],
  "method_summary": "方法概述（3-5句）",
  "main_results": "主要实验结果",
  "novelty_score": 8,
  "one_liner": "用一句话总结这篇论文的价值"
}}
```

论文内容:
{excerpt}
"""


def build_standard_prompt(text: str) -> str:
    excerpt = text[:12000]
    return f"""请对以下论文进行**八维度标准分析**，以结构化 Markdown 格式输出：

## 📌 基本信息
- 标题、作者、发表时间、期刊/会议、引用量（如有）

## 🎯 研究动机与问题定义
- 研究背景与现实意义
- 明确的研究问题
- 与已有工作的差距

## 🔧 方法论解析
- 核心技术路线
- 模型/算法结构
- 关键设计决策与理由

## 🧪 实验设计
- 数据集与评估指标
- 基准方法 (Baselines)
- 实验设置细节

## 📊 结果分析
- 主要性能指标
- 消融实验结论
- 可视化/案例分析

## 💡 创新点评估
- 技术创新点（打分 1-10）
- 工程价值（打分 1-10）
- 潜在应用场景

## ⚠️ 局限性与挑战
- 方法局限
- 实验局限
- 潜在失效场景

## 🔭 未来工作展望
- 论文中提及的方向
- 分析师建议的延伸方向

---
论文内容:
{excerpt}
"""


def build_deep_prompt(text: str) -> str:
    excerpt = text[:20000]
    return f"""请对以下论文进行**深度学术分析与批判性评估**，输出完整的研究报告：

---

# 深度分析报告

## 一、研究背景与问题域
详细阐述该问题的历史演进、现有方法的不足以及本文切入点。

## 二、技术贡献的深度解读
逐一分析每个贡献的技术原理、实现细节，评估其理论严谨性。

## 三、方法论的工程视角
从系统设计、计算复杂度、可扩展性、实际部署难度等工程角度评估。

## 四、实验的严格审查
- 数据集选择是否合理？
- 评估指标是否全面？
- 有无统计显著性检验？
- 基准方法是否公平？

## 五、结果可信度分析
分析结果的可复现性、泛化能力、潜在 cherry-picking 风险。

## 六、批判性评估
### 优势
### 不足与疑问
### 与同类工作的对比

## 七、学术影响力预测
- 引用潜力（1-10）
- 工业转化可能性
- 对领域的长期影响

## 八、研究价值总评
综合评分（满分100）与详细理由。

## 九、延伸阅读建议
相关领域的必读论文推荐（3-5篇）。

---
论文内容:
{excerpt}
"""


def build_compare_prompt(papers_info: list) -> str:
    papers_text = ""
    for i, p in enumerate(papers_info, 1):
        papers_text += f"\n\n### 论文 {i}: {p.get('title', f'Paper {i}')}\n"
        papers_text += p.get("text", "")[:4000]

    return f"""请对以下 {len(papers_info)} 篇论文进行**系统性对比分析**：

{papers_text}

---

请从以下维度输出对比报告：

## 📊 核心对比矩阵
| 维度 | {"  | ".join([f"论文{i}" for i in range(1, len(papers_info)+1)])} |
|------|{"-------|" * len(papers_info)}
| 问题域 | ... |
| 方法类型 | ... |
| 数据集 | ... |
| 核心指标 | ... |
| 创新程度 | ... |

## 🔍 方法论对比
各论文在方法上的异同，相互之间的改进关系。

## 📈 性能横向对比
在相同/相似任务上的性能差异，分析原因。

## 💡 互补性分析
这些工作如何相互补充？能否结合？

## 🏆 综合推荐
不同场景下应优先参考哪篇？

## 🔭 研究方向总结
这一系列工作指向的未来研究方向。
"""


def build_dimension_prompt(text: str, dimension: str) -> str:
    """针对特定维度的专项分析"""
    dim_map = {
        "methodology": "请深度解析该论文的**方法论**，包括技术原理、算法流程、模型架构、数学推导（如有）",
        "experiments": "请深度解析该论文的**实验设计与结果**，包括数据集、评估协议、基准对比、消融分析",
        "novelty": "请深度评估该论文的**创新性**，与先前工作的区别，贡献的原创程度和理论深度",
        "impact": "请深度分析该论文的**学术影响力与工业价值**，包括引用预期、实用性、开源生态",
        "limitations": "请深度挖掘该论文的**局限性与潜在问题**，包括方法缺陷、实验不足、理论漏洞",
    }
    instruction = dim_map.get(dimension, f"请深度分析该论文的 {dimension} 维度")
    return f"""{instruction}

论文内容:
{text[:15000]}
"""
